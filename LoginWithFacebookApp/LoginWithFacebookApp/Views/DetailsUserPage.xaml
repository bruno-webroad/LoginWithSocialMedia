<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LoginWithFacebookApp.Views.DetailsUserPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
            <Image Source="{Binding Photo}" WidthRequest="150" HeightRequest="150" HorizontalOptions="Center"/>
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                <Label Text="Nome:" />
                <Label Text="{Binding Name}" />
            </StackLayout>
            <Button HorizontalOptions="Center" Text="Logout" TextColor="White" BackgroundColor="Red" Command="{Binding LogoutCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>